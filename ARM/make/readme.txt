To setup:

The boot code came from:
http://github.com/raspberrypi/firmware/tree/master

sudo apt-get install build-essential git
#needed by cross compiler if running on x86-64!
sudo apt-get install ia32-libs

git clone git://github.com/raspberrypi/tools.git